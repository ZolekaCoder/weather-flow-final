# data/raw

SAWS / ERA5 files land here exactly as downloaded and are never modified.

Anything that needs cleaning or transforming goes through `src/data_prep/` and outputs to `data/processed/` instead.
